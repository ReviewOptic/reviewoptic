[[IgnoredVulns]]
id = "GHSA-3p68-rc4w-qgx5"
reason = "axios is overridden to 1.15.0 (fixed version) via npm overrides in package.json. The vulnerability exists in twilio's declared dependency spec but the installed version is safe."

[[IgnoredVulns]]
id = "GHSA-fvcv-3m26-pcqx"
reason = "axios is overridden to 1.15.0 (fixed version) via npm overrides in package.json. The vulnerability exists in twilio's declared dependency spec but the installed version is safe."
